ping:
  description: ping stuff
  params:
    host:
      description: who to ping
      type: string
      default: localhost
fail-before:
  description: action that fails before benchmark start
  params: {}
fail-during:
  description: action that fails during benchmark
  params: {}
fail-after:
  description: action that fails after benchmark finish
  params: {}
